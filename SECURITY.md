# حماية Daily Bake

ملخص طبقات الحماية الموجودة، وخطوتين متبقيتين محتاجة كونسول فايربيز.

## الطبقات الشغالة دلوقتي

### ١. قواعد قاعدة البيانات (الطبقة الأساسية)
الجذر مقفول (`.read: false` / `.write: false`) وكل عقدة بتفتح بالحد الأدنى المطلوب:

| العقدة | القراءة | الكتابة |
|---|---|---|
| `menu` `branches` `assets` | الكل (محتوى معروض للزوار) | أدمن بصلاحية المنيو/الفروع |
| `settings/features` `payments` `governorates` | الكل | المالك / صلاحية الإعدادات |
| `settings/telegram` (توكن البوت) | أدمن فقط | صلاحية الإعدادات |
| `admins` | أدمن فقط | المالك فقط |
| `orders` (القائمة) | أدمن فقط | — |
| `orders/{id}` (طلب واحد) | بالرقم فقط (للتتبّع) | الإنشاء مفتوح، التعديل لأدمن الطلبات |
| `orderProofs` (صور التحويل) | أدمن فقط | العميل يكتبها مرة واحدة |
| `subscribers` | أدمن فقط | حقول محدودة ومحدّدة الحجم |
| `backups` | المالك فقط | المالك فقط |

**قيود منع الإغراق والتزوير:**
- الطلب الجديد لازم يكون `createdAt` وقته الآن (±١٠ دقايق) — يمنع الحقن الجماعي بتواريخ مزوّرة.
- حدود على طول كل حقل: الاسم ٨٠، التليفون ٢٥، العنوان ٤٠٠، الملاحظات ٥٠٠ حرف.
- التقييم: المفتاح لازم يكون بصيغة صحيحة (أو رقم حساب الزائر)، والوقت لازم يكون الآن.
- الصور: `data:image/` فقط وبحد أقصى ~٩٠٠KB للصورة، والمفتاح لازم يكون بصمة hex.
- أي حقل جديد غير معروف في `subscribers` مرفوض إلا من أدمن.

### ٢. ربط الجهاز (لوحة التحكم)
كل حساب أدمن بيشتغل على جهاز واحد. الجهاز التاني بيتمنع ويقدر يطلب النقل، والمالك هو اللي يوافق.

- **فشل التحقق = منع الدخول** (مش سماح). لو القراءة فشلت بتظهر شاشة "تعذّر التحقق من الجهاز".
- **المالك نفسه** لما ينقل حسابه لجهاز جديد لازم إيميله يكون **متأكَّد** — الشرط ده متطبّق في القواعد كمان مش في الواجهة بس، فالباسورد لوحده مش كفاية للوصول من جهاز غريب.

### ٣. الواجهة
- كل نص جاي من مستخدم بيعدّي على `esc()` قبل العرض (يمنع حقن HTML/JS).
- كل رابط/صورة بتعدّي على `safeUrl()` (يمنع `javascript:` وغيره).
- ممنوع `onclick=` داخل HTML — الـ CSP بتمنع أي سكربت مضمّن.

### ٤. هيدرز الـ HTTP
`Content-Security-Policy` صارمة، `Strict-Transport-Security`، `X-Frame-Options: DENY`،
`X-Content-Type-Options`، `Referrer-Policy`، `Permissions-Policy`،
`Cross-Origin-Opener-Policy`، `Cross-Origin-Resource-Policy`.
لوحة التحكم كمان عليها `noindex` فمحركات البحث مابتفهرسهاش.

---

## خطوتان متبقيتان (من كونسول فايربيز)

### أ) تفعيل دخول الزوّار المجهول — يقفل تزوير التقييمات
دلوقتي الزائر مش مسجّل دخول، فالتقييم بيتربط برقم عشوائي في متصفحه — ممكن حد يخترع
أرقام كتير ويزوّر تقييمات. لما تفعّل الدخول المجهول بيبقى لكل زائر رقم من فايربيز نفسه،
والقواعد بتشترط إن التقييم يكون برقمه هو.

1. [Authentication → Sign-in method](https://console.firebase.google.com/project/mahbaz-12e0c/authentication/providers)
2. فعّل **Anonymous**

الكود جاهز — `ensureGuest()` في `js/firebase-config.js` بتشتغل تلقائياً أول ما تتفعّل،
ولو فضلت مقفولة الموقع بيشتغل عادي.

### ب) App Check — **ملغي**

اتجرّب واتشال. السبب: التطبيق كان مسجّل على **reCAPTCHA Enterprise** والمفتاح المتاح
**v3 كلاسيكي**، فالتبادل كان بيرجع `403 App attestation failed`، والمكتبة بتخزّن حالة
الفشل في IndexedDB لمدة ٢٤ ساعة وفايربيز بيرفض الاتصال بالقاعدة بسببها
(`Provided AppCheck credentials ... are invalid`) — يعني **لوحة التحكم ماكانتش تفتح**.

الكود اتشال بالكامل، وفي `js/firebase-config.js` تنضيف لمرة واحدة بيمسح البقايا
من متصفح أي مستخدم تلقائياً (`nb_appcheck_cleared_v1`).

لو حبيت ترجّعه مستقبلاً: لازم نوع المفتاح ونوع التسجيل في الكونسول يكونوا **متطابقين**،
ويتجرّب على صفحة العميل الأول قبل لوحة التحكم، وماينفعش يتفعّل Enforce غير بعد
ما التوكن يطلع سليم فعلاً.

---|---|
| `exchangeRecaptchaV3Token` | `400 App not registered` |
| `exchangeRecaptchaEnterpriseToken` | `403 App attestation failed` |
| `grecaptcha.enterprise.execute(key)` | `Invalid site key` |

ومع كل فشل المكتبة كانت **بتكبح المحاولات ٢٤ ساعة** وبتأخّر طلبات فايربيز —
وده كان بيمنع لوحة التحكم من الإقلاع. عشان كده اتقفل.

**للتشغيل لازم النوعين يتطابقوا — اختار واحد:**

**الأسهل (v3):** في [App Check → Apps](https://console.firebase.google.com/project/mahbaz-12e0c/appcheck/apps)
افتح التطبيق، وفي قسم **reCAPTCHA v3** (مش Enterprise) الصق **Secret key**
واضغط Save. بعدها في `js/appcheck.js`: `RECAPTCHA_MODE = 'v3'` و`APPCHECK_ENABLED = true`.

**الأفضل طويل المدى (Enterprise):** اعمل مفتاح Enterprise من
[Cloud Console → reCAPTCHA](https://console.cloud.google.com/security/recaptcha?project=mahbaz-12e0c)
وحطه في `RECAPTCHA_SITE_KEY` مع `RECAPTCHA_MODE = 'enterprise'` و`APPCHECK_ENABLED = true`.

بعد التفعيل تأكد إن التوكن بيتصدر، وبعدين (وبس بعدين) فعّل **Enforce**.

---

## صيانة دورية
- خد نسخة احتياطية من قسم "نسخة احتياطية" في اللوحة قبل أي تعديل كبير.
- راجع قائمة الأدمنز والأجهزة المربوطة كل فترة واشيل اللي مش محتاجه.
- لو باسورد أدمن اتسرّب: غيّره من Firebase Console، وافك ارتباط جهازه من اللوحة.
